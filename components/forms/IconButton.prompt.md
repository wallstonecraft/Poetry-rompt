Circular tap target for a single icon action (back, close, favorite, export). Always pass `aria-label`.

```jsx
<IconButton aria-label="Favorite poem" active={isFavorite}><HeartIcon/></IconButton>
```
